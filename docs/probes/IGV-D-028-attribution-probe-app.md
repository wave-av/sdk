# IGV-D-028 attribution probe (App-authored)

Throwaway probe: this PR exists only to measure how the `require_extra_approval_for_unattributed_changes` ruleset parameter treats a commit and PR authored by the wave-av-agent GitHub App (a non-Copilot App identity). It will be closed, never merged.
