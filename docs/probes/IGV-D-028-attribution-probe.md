# IGV-D-028 attribution probe (co-author trailer)

Throwaway probe: this PR exists only to measure whether the `require_extra_approval_for_unattributed_changes` ruleset parameter treats a commit whose author is a linked person and whose only other identity is a `Co-Authored-By` trailer as unattributed. It will be closed, never merged.
